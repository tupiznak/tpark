														/* Copyright 2016 Soldatov A. APO-13 */
												/* task 1_4 find max {GCD(A,B)}, where A + B = N	*/
#include <iostream>
#include <stdio.h>
/*******************************************************************************************************************************************************************************/
// This numbers are exeptions from Ferma`s theorem in range 1..10^7
/*
* #define Carmichael_number1 561
* #define Carmichael_number2 41041
* #define Carmichael_number3 825265
*/
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
//bool Ferma(int N);									// Ferma theoreme (is N prime? True if yes)
//int to_degree(int a, int N);							// fast degree (needed in Ferma)
//int Euclid											// Euclid algorithm
int Split_max(int *N);									// If N not prime checking for the greatest common devisor 
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
int main(void) {
    int N;
	int tempN;
    int A, B;
	
	std::cin >> N;										// scanf is not working with my compiler, scanf_s is not working with edjudge compiler,
														// so that's why I mixed c/c++ IO functions
	tempN = N;

	Split_max(&tempN);
	if (tempN == N){
		A = tempN - 1;
		B = tempN - A;
	}
	else {
		A = tempN;
		B = N - A;
	}

	if (A > B){
		tempN = A;
		A = B;
		B = tempN;
	}

	printf("%u %u", A, B);

// Ferma test for prime numbers (TODO)
/* if (Ferma(N)){
*		printf("%u %u", (1), (N-1));
*	}
*/

// Euclid algorithm (run-time error, must change for loop)
/*
*	int trueA, trueB, temp_GCD, max_GCD = 0;
*
*	for (A = 2; A <= (N / 2); ++A) { // could we use prime numbers table here? (see prime.txt)
*		B = N - A;
*		temp_GCD = Euclid(A, B);
*		if (max_GCD < temp_GCD) {
*			max_GCD = temp_GCD;
*			trueA = A;
*			trueB = B;
*		}
*	}
*/
}
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
int Split_max(int *N){
	for (int devider = 2; devider * devider <= *N; ++devider)
		if (*N % devider == 0){
			*N /= devider;
			return *N;
		}
}
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
// fast degree (in progress, see 1st lection)
/*
*/
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
// Ferma test
/*
* bool Ferma(int N){
*	int a = 3;
*	int temp = to_degree(a,N);
*	if (((temp % N) == (a % N)) && (N != Carmichael_number1) && (N != Carmichael_number2) && (N != Carmichael_number3))
*		return true;
*	else
*		return false;
*}
*/
/*******************************************************************************************************************************************************************************/
/*******************************************************************************************************************************************************************************/
// Euclid algorithm
/*
* int Euclid(int A, int B) {
*	if (B == 0)
*		return A;
*	return Euclid(B, A % B);
* }
*/
/*******************************************************************************************************************************************************************************/
